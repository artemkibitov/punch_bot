# 🚀 EPIC: Quick Start Guide

## Для команды разработчиков

---

## 📋 ЧТО ДЕЛАТЬ ПЕРВЫМ

### Backend Dev #1: Миграции и репозитории

**Приоритет 1:**
1. Создать миграцию 012: добавить роль ADMIN
2. Создать миграцию 013: расширить `work_objects` (timezone, planned_start, planned_end, lunch_minutes)
3. Создать миграцию 014: создать таблицу `work_logs`
4. Создать миграцию 015: обновить `shifts` (status, started_at, closed_at, confirmed_by)
5. Создать миграцию 016: создать таблицу `audit_logs`
6. Создать миграцию 017: расширить `employees` (status, ref_code, ref_code_expires_at)

**Приоритет 2:**
1. Создать `ObjectRepository` (базовые CRUD)
2. Создать `AssignmentRepository` (assign/remove)
3. Создать `ShiftRepository` (create, confirmStart, confirmEnd)
4. Создать `WorkLogRepository` (create, updateEnd, createOverride)
5. Создать `AuditLogRepository` (log, findByEntity)

**Критерий готовности:** Все миграции проходят, репозитории возвращают данные.

---

### Backend Dev #2: Сервисы и бизнес-логика

**Приоритет 1:**
1. Создать `ObjectService.create()` - создание объекта с валидацией
2. Создать `ShiftService.confirmStart()` - подтверждение начала смены
3. Создать `ShiftService.confirmEnd()` - подтверждение окончания смены
4. Создать `WorkLogService.createForShift()` - создание work_logs для всех сотрудников
5. Создать `WorkLogService.updateEnd()` - закрытие work_logs

**Приоритет 2:**
1. Создать `EmployeeService.assignToObject()` - назначение сотрудника
2. Создать `ReferralService.generateToken()` - генерация ref-ссылки
3. Создать `ReferralService.activateToken()` - активация ref-ссылки
4. Интегрировать `AuditLogRepository` во все сервисы

**Критерий готовности:** Все сервисы покрыты unit-тестами, логика прав доступа работает.

---

### FSM Dev: Состояния и сценарии

**Приоритет 1:**
1. Создать `managerObjectsList.js` - список объектов менеджера
2. Создать `objectCreateEnterName.js` - ввод названия объекта
3. Создать `objectCreateEnterSchedule.js` - ввод графика объекта
4. Создать `shiftStartConfirmation.js` - подтверждение начала смены
5. Создать `shiftStartEnterAbsentIds.js` - ввод ID отсутствующих

**Приоритет 2:**
1. Создать `shiftEndConfirmation.js` - подтверждение окончания смены
2. Создать `shiftEndEnterOvertimeIds.js` - ввод ID дорабатывающих
3. Создать `shiftEndEnterOvertimeTime.js` - ввод времени окончания
4. Создать `employeeRefLinkActivate.js` - активация ref-ссылки
5. Обновить `transitions.js` - добавить все новые переходы

**Критерий готовности:** Все сценарии проходят end-to-end, валидация работает.

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Структура файлов

```
app/
├── application/
│   ├── fsm/
│   │   ├── states/
│   │   │   ├── managerObjectsList.js
│   │   │   ├── objectCreateEnterName.js
│   │   │   ├── objectCreateEnterSchedule.js
│   │   │   ├── shiftStartConfirmation.js
│   │   │   └── ... (остальные состояния)
│   │   ├── registry.js
│   │   └── router.js
│   └── services/
│       ├── objectService.js
│       ├── shiftService.js
│       ├── workLogService.js
│       └── ... (остальные сервисы)
├── infrastructure/
│   ├── repositories/
│   │   ├── objectRepository.js
│   │   ├── assignmentRepository.js
│   │   ├── shiftRepository.js
│   │   ├── workLogRepository.js
│   │   └── auditLogRepository.js
│   └── scheduler/
│       └── shiftTrigger.js
└── domain/
    └── fsm/
        ├── states.js (обновить)
        └── transitions.js (обновить)
```

---

## ✅ CHECKLIST ПЕРЕД КОММИТОМ

### Backend Dev #1
- [ ] Миграции проходят без ошибок
- [ ] Все репозитории имеют базовые методы
- [ ] Репозитории возвращают правильные данные
- [ ] Проверка прав доступа работает (managerId фильтр)

### Backend Dev #2
- [ ] Все сервисы покрыты unit-тестами
- [ ] Логика прав доступа работает корректно
- [ ] Audit logs фиксируют все изменения
- [ ] Обработка ошибок работает везде

### FSM Dev
- [ ] Все состояния регистрируются
- [ ] Все transitions корректны
- [ ] Валидация работает во всех состояниях
- [ ] Обработка ошибок корректна
- [ ] Callback handlers созданы

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

1. **Триггеры смен:** Нужно решить, как запускать (cron job или другой механизм)
2. **Timezone:** Нужно проверить работу с timezone в PostgreSQL
3. **Ref-ссылки:** Нужно решить формат URL для Telegram

---

## 📞 КОНТАКТЫ

При вопросах обращаться к Tech Lead.

---

**Последнее обновление:** 2025-01-XX

